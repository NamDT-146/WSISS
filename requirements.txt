# WSSIS pip requirements
# Install order: bash scripts/setup/00_create_conda_env.sh
#   1) PyTorch (cu124) from PyTorch index
#   2) This file
#   3) Detectron2 (editable, modules/detectron2) + Mask2Former CUDA ops
#
# Tested: Python 3.10, torch 2.6.0+cu124, torchvision 0.21.0+cu124, detectron2 0.6

# Core
numpy>=1.24,<3
scipy>=1.11
Pillow>=10.0
tqdm>=4.66
PyYAML>=6.0
matplotlib>=3.8
opencv-python-headless>=4.8
pandas>=2.0

# COCO & vision
pycocotools>=2.0.7
scikit-image>=0.22
timm>=0.9.12
h5py>=3.10
shapely>=2.0
cython>=3.0

# Experiment tracking
tensorboard>=2.14
wandb>=0.16

# Kaggle downloads
kaggle>=1.6

# Hugging Face Hub (upload Exp 1C best weights)
huggingface_hub>=0.20

# Stage-2 students (installs opencv-python; headless above is preferred on servers)
ultralytics>=8.1

# SAM (teacher) — PyPI release matches Meta segment_anything v1.0
segment-anything>=1.0

# Detectron2 / Mask2Former (detectron2 itself: setup script, not this file)
fvcore>=0.1.5,<0.1.6
iopath>=0.1.7,<0.1.10
omegaconf>=2.1,<2.4
hydra-core>=1.1
yacs>=0.1.8
cloudpickle
termcolor
tabulate
portalocker
submitit>=1.5

# Optional: panoptic API for some Detectron2 datasets
# git+https://github.com/cocodataset/panopticapi.git
